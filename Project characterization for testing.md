# אפיון פקד PersonLocator \u200F(@ips/searchAdam) — מסמך לצוות הבדיקות

<div dir="rtl">

## 1. מהות הפקד ומשמעותו

**PersonLocator** הוא פקד React גנרי (ספריית קומפוננטות) לחיפוש ואיתור אנשים בארגון.
הפקד כתוב ב-TypeScript, ממשק המשתמש בעברית ובכיוון RTL, וה-CSS מוזרק אוטומטית (אין צורך בהגדרות Tailwind אצל הצרכן).

הפקד מחפש בו-זמנית בשלוש קטגוריות של אנשים:

| קטגוריה | ערך טכני | הערות |
|---|---|---|
| אסיר | `asir` | נתמך גם מקור אונליין, מידור (whitelist) והרשאת איתור |
| סוהר | `soher` | נתמך גם מקור אונליין |
| אזרח | `ezrach` | Elasticsearch בלבד (אין שירות אונליין) |

החיפוש מתפזר במקביל לשלושה מקורות מידע:

1. **Elasticsearch** — המקור הראשי, נשלח תמיד, לכל סוג אדם בנפרד (אינדקסים: `asirs`, `sohers`, `ezrachs`).
2. **שירות אונליין** — במקביל, רק עבור אסיר וסוהר, ורק בעמוד הראשון (לא ב"טען עוד").
3. **DB לא-מקוון (Offline)** — כגיבוי אוטומטי כאשר Elasticsearch נופל עבור סוג מסוים, או כאשר אין הרשאת איתור לסוג. תומך רק בחיפוש לפי מזהה מספרי מדויק (ספרות בלבד).

התוצאות משלושת המקורות ממוזגות עם הסרת כפילויות לפי `id` (בכפילות — תוצאת האונליין גוברת על Elasticsearch), מועשרות בתמונות, ומוצגות בטאבים לפי סוג אדם.

## 2. יכולות עיקריות (מה הפקד יודע לעשות)

### 2.1 זרימת חיפוש

- **Debounce של 300ms** — החיפוש יוצא רק לאחר 300ms של שקט בהקלדה.
- **מינימום תווים** — ברירת מחדל 3 תווים (`minChars`); מתחת לזה לא יוצא חיפוש והתוצאות מתרוקנות.
- **ביטול בקשות** — כל הקלדה חדשה מבטלת (AbortController) את כל הבקשות שבדרך מההקלדה הקודמת. תשובה איטית/ישנה לעולם לא תדרוס תשובה חדשה.
- **נרמול מקלדת אנגלית→עברית** — אם המשתמש השאיר את המקלדת באנגלית והקליד מילה עברית (לפי מיקום פיזי של המקשים, למשל `sbh` → `דני`), הקלט מומר אוטומטית לעברית **פעם אחת**, לפני שהוא נשלח לכל המקורות. ספרות, רווחים, מקפים ועברית עוברים ללא שינוי.
- **פיצול מונחים** — שאילתה מרובת מילים מפוצלת לפי רווחים ומחוברת ב-AND. מצב העטיפה שונה בין סוגים: אסיר — `prefix` (השלמה בסוף), סוהר — `wildcard` (הכלה, `*טקסט*`), אזרח — `exact` (התאמה מדויקת). **דגש בדיקות: התנהגות החיפוש החלקי שונה בין הטאבים בכוונה.**
- **שדות חיפוש ברירת מחדל**: אסיר — `fullName`, `idNumber`, `unit`, `prisonerNumber`; סוהר — `fullName`, `rank`, `unit`, `phone`; אזרח — `fullName`, `idNumber`, `phone`.

### 2.2 תצוגת תוצאות

- תוצאות מקובצות בטאבים לפי סוג אדם; **הטאב הראשון שיש בו תוצאות נפתח אוטומטית** (סדר קדימויות: אסיר ← סוהר ← אזרח).
- **גלילה אינסופית** (IntersectionObserver) — הגעה לסוף רשימת הטאב טוענת עמוד נוסף עבור אותו טאב בלבד. גודל עמוד ברירת מחדל: 5 (`pageSize`).
- **ספירת תוצאות** — סה"כ לכל טאב כולל את ספירת ה-ES בתוספת אנשים שהגיעו רק מהשירות האונליין.
- **כרטיס תוצאה** — שם (עם הדגשת הטקסט התואם), תמונה, אייקון לפי סוג, סטטוס פעיל/לא פעיל (Badge), שדות מורחבים (הרחבה/כיווץ), כפתורי פעולה: פתיחת "תיק אסיר" (`openTikAsir`), קישורי ניווט (`navigate`).
- **מצבי תצוגה**: Skeleton בזמן טעינה, EmptyState כשאין תוצאות, באנר צהוב במצב לא-מקוון.
- הדרופדאון נפתח למטה או למעלה לפי `resultDirection`, והוא מוצג ב-portal על `<body>` כדי לא להיחתך על ידי `overflow:hidden` של אלמנטים עוטפים. **דגש בדיקות: לחיצה מחוץ לפקד סוגרת את הפאנל — כולל כשהפאנל עצמו ב-portal.**

### 2.3 מצב לא-מקוון (Offline Fallback)

אין prop להפעלה — הנפילה ל-offline אוטומטית:

- כל כשל של Elasticsearch עבור סוג מסוים (שגיאת רשת, timeout — ברירת מחדל 5000ms, \u200F4xx/5xx) מפעיל fallback **רק עבור אותו סוג**. ביטול (Abort) אינו נחשב כשל.
- מופיע באנר צהוב: **"מצב לא מקוון – ניתן לחפש לפי מספר מזהה מדויק בלבד"**.
- ה-DB הלא-מקוון נשאל **רק כאשר הטקסט שהוקלד הוא מספר בלבד** (ספרות בלבד, אורך כלשהו). טקסט חופשי לא יחזיר תוצאות offline.
- תוצאות offline **לעולם לא כוללות תמונה**, גם אם השרת החזיר אחת (השדה מוסר בצד הלקוח).
- הבאנר מוצג רק בנפילת ES אמיתית — fallback בגלל חוסר הרשאה (סעיף 2.4) **אינו** מציג באנר.

### 2.4 הרשאות ומידור

- **מידור אסירים (Whitelist)** — אם מוגדר endpoint\u200F `asirWhitelist`, נשלפת פעם אחת בעליית הפקד רשימת מזהי אסירים שהמשתמש רשאי לראות, ומוזרקת כפילטר `terms` לשאילתת ה-ES של אסירים וכן לקריאת האונליין. כשל בשליפה → fail-open (ללא מידור).
- **הרשאת איתור (`fallbackToOfflineIfNoAuth`)** — כאשר `true`, נבדק פעם אחת בעליית הפקד endpoint\u200F `asirPermission`. סוג אדם שאין למשתמש הרשאה עבורו (403 / רשימת מורשים ריקה) — ה-ES מדולג עבורו והחיפוש עובר ל-offline. אם בדיקת ההרשאה עצמה נכשלת — fail-open וה-ES נשאל כרגיל.
- **קונפיגורציית שדות מרחוק (`fieldConfig`)** — השרת יכול לדרוס את `searchFields`/`sourceFields` לכל סוג ללא דיפלוי לקוח. השליפה מתבצעת בעליית הפקד, והחיפוש הראשון **ממתין לה** כדי לא לחפש עם שדות מיושנים.
- סדר השכבות על הגדרות השאילתה: מידור ← קונפיגורציית שדות מרחוק ← `additionalSourceFields` מה-props.

### 2.5 סינון (Filters)

- `filters` — פילטר משתמש; `baseFilter` — פילטר מערכתי מחייב (תיחום הרשאות). `baseFilter` תמיד מחובר ב-AND עם `filters` — פילטר משתמש יכול לצמצם אך **לעולם לא להרחיב או לעקוף** אותו.
- אבני הבניין: `eq`, `oneOf`, `noneOf`, `range`, `exists`, `and`, `or`, `not`. אי אפשר להעביר שאילתת ES גולמית.
- עץ הפילטרים מקומפל **פעם אחת לשני מהדרים**: סעיף ES ופרדיקט צד-לקוח — כך ששלושת המקורות (ES / אונליין / offline) תמיד מסכימים מי עובר את הסינון. **דגש בדיקות מרכזי: אותו פילטר חייב להחזיר את אותה קבוצת אנשים בכל שלושת המקורות, כולל ב-singleSearch.**
- סמנטיקת שדה חסר זהה ל-ES: שדה חסר ברשומה → `eq`/`oneOf`/`range`/`exists` פוסלים אותה, `noneOf` מקבל אותה.
- שם השדה בפילטר חייב להיות השם השטוח מה-ES (למשל `unit`) ולא `data.unit` — כתיבת `data.` תעבוד בצד הלקוח אך תיכשל בשקט מול ES.
- ערכי מחרוזת מכוונים אוטומטית ל-`<field>.keyword`.
- מעל 1024 ערכים ב-`oneOf`/`noneOf` — נזרקת שגיאה (`MAX_INLINE_TERMS`).

### 2.6 חיפושים אחרונים (Recent Searches)

- כל בחירת אדם נשמרת ב-`localStorage` תחת המפתח `person-locator:recent` — עד **5 פריטים**, ללא כפילויות לפי `id` (בחירה חוזרת מקפיצה לראש הרשימה).
- הרשימה מוצגת כאשר השדה בפוקוס וריק, מסוננת לפי ה-`type` הנוכחי של הפקד.
- ניתן להסיר פריט בודד או לנקות הכול.
- רשומות פגומות ב-localStorage (JSON שבור, פריטים לא תקינים) מסוננות בשקט ואינן מפילות את הפקד.

### 2.7 מצבים נוספים

- **`singleSearch`** — אכלוס מראש בעליית הפקד: שליפת אדם יחיד לפי שדה+ערך מדויקים (ES + אונליין במקביל; אונליין גובר). אם לא נמצא ו-`fallbackToOfflineIfNoAuth` פעיל — ניסיון גם מול offline. הפילטרים חלים גם כאן. השדה נשאר ניתן לעריכה לאחר האכלוס.
- **מצב מבוקר (`state`)** — האב יכול לשלוט על האדם הנבחר; העברת `null` מנקה את הפקד מבחוץ.
- **מתג פעיל/לא פעיל** — ברירת מחדל: מציג פעילים בלבד. `isDefaultActive` קובע ערך התחלתי; `activeOnly` נועל לפעילים בלבד **ומסתיר את המתג לגמרי** (וגובר על `isDefaultActive`).
- **בחירת אדם** — ממלאת את השדה בשם המלא, מפעילה `onSelect`, ולא מפעילה חיפוש חוזר על השם שהוזן (דגל פנימי מונע זאת).
- **ניקוי** — מרוקן קלט, תוצאות, בחירה, שגיאות ומצב offline, ומפעיל `onClear`.

## 3. אפשרויות ההעברה לפקד (Props)

ה-prop היחיד שהוא **חובה** הוא `env`.

| Prop | טיפוס | ברירת מחדל | תיאור |
|---|---|---|---|
| `env` | `'dev' \| 'test' \| 'lrn' \| 'prod'` | **חובה** | סביבת עבודה — קובעת את כתובות השרתים מהקונפיגורציה הפנימית. |
| `type` | סוג יחיד או מערך | — | תיחום החיפוש. סוג יחיד: נועל את הפקד (מסתיר טאבים ופילטר סוג). מערך של 2: מציג רק את הטאבים האלה ומשאיר פילטר סוג פתוח. ללא — כל השלושה. |
| `minChars` | `number` | `3` | מינימום תווים להפעלת חיפוש. |
| `onSelect` | `(person) => void` | — | נורה בבחירת אדם מהתוצאות או מהחיפושים האחרונים. |
| `disabled` | `boolean` | — | נטרול מלא של הפקד (`pointer-events: none`). |
| `resultDirection` | `'up' \| 'down'` | `'down'` | כיוון פתיחת הדרופדאון. |
| `additionalSearchFields` | `string[]` | `[]` | **לא פעיל בגרסה זו** — מתקבל אך לא מוחל על שאילתת ES. להוספת שדות חיפוש יש לדרוס `serviceConfig.querySettings[type].searchFields`. **דגש בדיקות: לוודא שאין ציפייה שה-prop הזה משפיע.** |
| `additionalSourceFields` | מפה לפי סוג | — | שדות `_source` נוספים לשליפה והצגה בכרטיס המורחב, לכל סוג. מתמזגים על ברירת המחדל — שדות מובנים לא נמחקים. |
| `filters` | פילטר או מערך | — | פילטר משתמש (ראו סעיף 2.5). |
| `baseFilter` | פילטר או מערך | — | פילטר מערכתי מחייב — תמיד AND עם `filters`. |
| `fallbackToOfflineIfNoAuth` | `boolean` | `false` | בדיקת הרשאת איתור בעלייה; סוג ללא הרשאה מחופש offline (ראו סעיף 2.4). |
| `singleSearch` | `{ key, value }` | — | אכלוס מראש לפי התאמת שדה/ערך מדויקת. |
| `state` | `PersonResult \| null` | — | ערך נבחר מבוקר; `null` מנקה מבחוץ. |
| `openTikAsir` | `(person) => void` | — | פתיחת מערכת "תיק אסיר" מכפתור בכרטיס. |
| `onClear` | `() => void` | — | נורה בניקוי שדה החיפוש (ידני או תוכניתי). |
| `navigate` | `(path) => void` | — | ניווט (בד"כ react-router) מכפתורי הכרטיס. |
| `HidePhotosSugAdam` | מערך סוגים | — | הסתרת תמונות בכרטיסים עבור הסוגים שצוינו. |
| `displayIdNumber` | מערך סוגים | — | הצגת ת"ז בכרטיס עבור הסוגים שצוינו (מוסתר כברירת מחדל לכולם). |
| `HideMishmorot` | `boolean` | — | הסתרת נתוני משמרת/טלפון בכרטיסים. |
| `hideNavigationLinks` | `boolean` | — | הסתרת כל כפתורי הניווט בכרטיסים. |
| `activeOnly` | `boolean` | — | פעילים בלבד + הסתרת המתג. |
| `isDefaultActive` | `boolean` | — | ערך התחלתי של המתג (רלוונטי רק כש-`activeOnly` לא הועבר). |
| `serviceConfig` | `Partial<ServiceConfig>` | — | דריסת קונפיגורציית השירותים (URLs, `pageSize`, `timeoutMs`, `querySettings`, טוקן). שימושי למוקים ולבדיקות. |

### קונפיגורציית שירותים (`serviceConfig`)

- `pageSize` — ברירת מחדל 5; `timeoutMs` — ברירת מחדל 5000.
- `authToken` — Bearer token לכל הקריאות.
- ניתן לדרוס כתובות בסיס ונתיבים לכל שירות: `elasticsearch`, `online`, `offline`, `photos`, `asirWhitelist` (מידור), `asirPermission` (הרשאת איתור), `fieldConfig`.
- הדריסה היא shallow-merge על קונפיגורציית הסביבה המובנית — **מומלץ לצוות הבדיקות להשתמש בזה כדי להפנות את הפקד לשרתי Mock.**

## 4. דגשים ותרחישים לבדיקות

### 4.1 קלט וחיפוש בסיסי

1. הקלדת פחות מ-`minChars` תווים — לא יוצאת אף בקשת רשת; תוצאות קודמות מתרוקנות.
2. הקלדה מהירה רציפה — יוצאת בקשה אחת בלבד לאחר 300ms שקט; לוודא (בכלי פיתוח/Mock) שהבקשות הקודמות בוטלו.
3. **מרוץ תשובות**: לגרום לתשובה של שאילתה ישנה להגיע אחרי שאילתה חדשה (השהיה מלאכותית ב-Mock) — התוצאות המוצגות חייבות להתאים לטקסט הנוכחי בלבד.
4. הקלדה במקלדת אנגלית של מילה עברית (למשל `sbh`) — החיפוש מתבצע על ההמרה העברית (`דני`); לוודא שההדגשה בתוצאה ותצוגת השאילתה עקביות. קלט מספרי/עברי — עובר כמו שהוא.
5. חיפוש רב-מילי (רווחים) — כל המילים חייבות להתאים (AND).
6. השוואת התנהגות חיפוש חלקי בין הסוגים: אסיר — תחיליות; סוהר — הכלה באמצע מילה; אזרח — התאמה מדויקת בלבד.

### 4.2 טאבים, דפדוף וגלילה

7. חיפוש שמחזיר תוצאות רק בסוג אחד — הטאב שלו נפתח אוטומטית; סדר עדיפות אסיר←סוהר←אזרח כששניים ומעלה מלאים.
8. גלילה לסוף רשימה — נטען עמוד נוסף רק לטאב הפעיל, ה-offset מתקדם נכון (עמוד ראשון 0, שני `pageSize` וכו'), אין כפילויות ואין דילוגים.
9. ב"טען עוד" **לא** יוצאת קריאה לשירות האונליין (רק ES) — והספירה הכוללת לטאב נשארת יציבה (התוספת מהאונליין מהעמוד הראשון נשמרת).
10. החלפת טאב תוך כדי טעינת "עוד" — לוודא שאין ערבוב תוצאות בין טאבים.
11. `pageSize` דרוס דרך `serviceConfig` — מכובד בפועל.

### 4.3 מצב לא-מקוון והרשאות

12. הפלת ES לסוג אחד בלבד (למשל 500 באינדקס אסירים) — באנר מופיע, אסירים נשאלים מ-offline, ואילו סוהר/אזרח ממשיכים לעבוד רגיל מ-ES.
13. במצב offline: קלט מספרי בלבד מחזיר תוצאות; טקסט חופשי — רשימה ריקה (ולא שגיאה).
14. תוצאת offline לא מציגה תמונה בשום מקרה, גם אם השרת החזיר `photoUrl`.
15. Timeout (מעל 5000ms) מתנהג ככשל ES ומפעיל fallback; ביטול עקב הקלדה חדשה — **לא** מפעיל fallback ולא באנר.
16. `fallbackToOfflineIfNoAuth=true` + \u200F403 מהשרת עבור סוג — אותו סוג מחופש offline **ללא באנר**; סוגים מורשים ממשיכים ב-ES.
17. כשל בבדיקת ההרשאה עצמה / בשליפת המידור / בקונפיגורציית השדות — fail-open: הפקד ממשיך לתפקד רגיל.
18. מידור: משתמש עם whitelist — תוצאות אסירים כוללות אך ורק מזהים מהרשימה, גם מהמקור האונליין.
19. ניקוי הקלט או ירידה מתחת ל-`minChars` — הבאנר ומצב ה-offline מתאפסים.

### 4.4 מיזוג מקורות

20. אדם שקיים גם ב-ES וגם באונליין (אותו `id`) — מופיע פעם אחת, עם נתוני האונליין (האונליין גובר).
21. אדם שקיים רק באונליין — מופיע, ונספר בסה"כ של הטאב מעבר לספירת ה-ES.
22. אזרחים — לוודא שלא יוצאת עבורם קריאה לשירות האונליין כלל.
23. כשל של השירות האונליין בלבד (ES תקין) — התוצאות מ-ES מוצגות רגיל, ללא באנר וללא שגיאה למשתמש.

### 4.5 פילטרים

24. `baseFilter` + `filters` — לוודא שפילטר משתמש לא מסוגל להחזיר רשומה שה-`baseFilter` פוסל (תיחום הרשאות).
25. אותו פילטר על שלושת המקורות: ES, אונליין (Mock שמחזיר רשומות שלא עוברות את הפילטר) ו-offline — הרשומות הפסולות לא מוצגות מאף מקור.
26. שדה חסר ברשומה: `eq`/`oneOf`/`range`/`exists` פוסלים, `noneOf` מקבל.
27. פילטר עם שם שדה `data.<field>` — צפוי לא להתאים כלום מול ES (התנהגות מתועדת, לא באג).
28. מעל 1024 ערכים ב-`oneOf` — נזרקת שגיאה ברורה.
29. שינוי דינמי של `filters` בזמן שיש שאילתה פעילה — החיפוש רץ מחדש אוטומטית.

### 4.6 חיפושים אחרונים

30. בחירת אדם → מופיע בראש הרשימה בפוקוס הבא על שדה ריק; בחירה שישית מוחקת את הישן ביותר (מקסימום 5).
31. בחירה חוזרת של אותו אדם — לא נוצרת כפילות, הפריט קופץ לראש.
32. פקד עם `type` מתוחם — מוצגים רק "אחרונים" מהסוגים המותרים.
33. localStorage עם JSON שבור / פריטים חלקיים — הפקד עולה תקין והרשומות הפגומות מסוננות.
34. הסרת פריט בודד וניקוי כולל — משתקפים גם לאחר רענון דף (persistence).

### 4.7 מצבים מבוקרים ואכלוס מראש

35. `singleSearch` — הפקד עולה מאוכלס; אם קיימת התאמה גם באונליין וגם ב-ES, מוצגת גרסת האונליין; אם האדם לא עובר את `baseFilter` — הפקד נשאר ריק.
36. `state` — עדכון מבחוץ משנה את הבחירה; `state={null}` מנקה; לוודא שאין לולאת עדכון עם `onSelect`.
37. בחירת אדם ממלאת את השדה בשמו — לוודא ש**לא** יוצא חיפוש חדש על השם שמולא.
38. ניקוי (X) — מרוקן הכול ומפעיל `onClear` פעם אחת.

### 4.8 תצוגה, RTL ונגישות

39. כלל הממשק בעברית ו-RTL — יישור, כיוון אייקונים, הדגשת טקסט תואם בשם.
40. `resultDirection='up'` — הפאנל נפתח כלפי מעלה ואינו נחתך; לבדוק גם בתוך container עם `overflow:hidden` (הפאנל ב-portal).
41. לחיצה מחוץ לפקד סוגרת את הפאנל; לחיצה בתוך הפאנל (שנמצא ב-portal) — לא סוגרת.
42. `disabled` — אין אינטראקציה כלל.
43. מתג פעיל/לא פעיל: שינוי מריץ חיפוש מחדש; `activeOnly` מסתיר את המתג; `isDefaultActive=false` פותח במצב "כולם".
44. props תצוגה: `HidePhotosSugAdam`, `displayIdNumber`, `HideMishmorot`, `hideNavigationLinks` — כל אחד משפיע רק על מה שהוגדר לו ולסוגים שהוגדרו.
45. מקלדת: ניווט ובחירה ב-Enter/רווח על כרטיס תוצאה.
46. תמונות: כשל בשירות התמונות לא מפיל את התצוגה (מוצג placeholder); אזרח עם `HidePhotosSugAdam=['ezrach']` — ללא תמונה.

### 4.9 סביבות ותשתית

47. כל `env` פונה לכתובות הנכונות שלו; `serviceConfig` דורס נקודתית בלבד (shallow-merge).
48. `authToken` — נשלח כ-Bearer בכל הקריאות (כולל תמונות, מידור, הרשאות).
49. **הערה לסביבת dev**: קיימים כיום לוגים של `console.log` (דיבוג זמני של הפילטרים) שמודפסים רק ב-`env='dev'` — אינם תקלה, אך אמורים לא להופיע בשאר הסביבות.
50. Unmount של הפקד באמצע חיפוש — כל הבקשות מבוטלות, אין שגיאות בקונסול.

## 5. סיכום נקודות רגישות (סדר עדיפות מומלץ לבדיקה)

1. **עקביות סינון בין שלושת המקורות** ואי-עקיפות של `baseFilter` — קריטי (תיחום הרשאות).
2. **מידור והרשאת איתור** — דליפת אסיר שאינו ברשימת המידור היא התקלה החמורה ביותר האפשרית בפקד.
3. **מצב offline** — נפילה לכל סוג בנפרד, באנר, מגבלת מזהה מספרי, ללא תמונות.
4. **מרוצי בקשות וביטולים** — יציבות תחת הקלדה מהירה ורשת איטית.
5. **מיזוג וספירה** — כפילויות, "אונליין גובר", יציבות המונה ב"טען עוד".
6. **דגלים ידועים**: `additionalSearchFields` לא פעיל בגרסה זו; לוגי דיבוג ב-dev.

</div>
